# fastapi_first_try


first you need to initiate "uv"
that will create the necessery files

then run uv add <dependencies = [
    "aiosqlite>=0.21.0",
    "fastapi>=0.121.1",
    "fastapi-users[sqlalchemy]>=15.0.1",
    "imagekitio>=4.2.0",
    "python-dotenv>=1.2.1",
    "streamlit",
    "uvicorn[standard]>=0.38.0",
]>

run them one by one
